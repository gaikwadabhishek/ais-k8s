name: Publish ais-k8s release 

on:
  workflow_dispatch:
    inputs:
      version:
        description: "The version to release. Must be a tag already available in github. eg. v0.95"
        required: true


env:
  AISOPERATOR_IMAGE: 'gaikwadabhishek/ais-operator'
  VERSION: ${{ input.version }}

jobs:
  release:
    runs-on: ubuntu-22.04
    steps:

    - uses: actions/checkout@v3
    - uses: actions/setup-go@v4
      with:
        go-version: '>=1.21.0'
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: make aisoperator image
      run: |
        pushd $GITHUB_WORKSPACE/operator
        IMG="${{ env.AISOPERATOR_IMAGE }}:${{ env.VERSION }}" make docker-build docker-push
        popd
  
    - name: make operator yaml file
      run: |
        pushd $GITHUB_WORKSPACE/operator
        VERSION="${{ env.VERSION }}" IMG="${{ env.AISOPERATOR_IMAGE }}:${{ env.VERSION }}" make bundle-manifests
        popd

    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        files: operator/dist/*
